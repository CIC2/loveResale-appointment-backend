apiVersion: apps/v1
kind: Deployment
metadata:
  name: resale-appointment-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resale-appointment-app
  template:
    metadata:
      labels:
        app: resale-appointment-app
    spec:
      containers:
        - name: resale-appointment-app
          image: de.icr.io/dev-registry/loveresale-appointment-backend:latest
          args:
            - --spring.application.json=$(BINDING)
          env:
            - name: BINDING
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: application-properties
            - name: DB_CONNECTION
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: DB_CONNECTION
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: DB_PASSWORD
          ports:
            - containerPort: 8025
          resources:
            requests:
              cpu: 125m
              memory: 350Mi
---
apiVersion: v1
kind: Service
metadata:
  name: resale-appointment-service-cip
  labels:
    app: resale-appointment-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8025
  selector:
    app: resale-appointment-app
